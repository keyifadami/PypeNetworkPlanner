class Node(object):
    def __init__(self, pos, label, name="Unnamed"):
        self.label = label
        self.pos = pos #a position on the cartesian plane
        self.name = name

## this module is alittle bit barren right now but it'll
## get more stuff once I start thinking about different types of nodes
## which will happen later
